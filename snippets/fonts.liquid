{% comment %}
  {% style %}
    @font-face {
      font-family: 'TimezNewRoman';
      src: url({{ 'TimezNewRoman.woff2' | asset_url }}) format('woff2'),
           url({{ 'TimezNewRoman.woff' | asset_url }}) format('woff');
      font-weight: normal;
      font-style: normal;
      font-display: swap;
    }
  {% endstyle %}
{% endcomment %}
